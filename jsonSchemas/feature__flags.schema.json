{
    "type": "object",
    "properties": {
        "accept_kontakt_msgs": {
            "type": ["object"],
            "properties": {
                "serverAccept": {
                    "type": "boolean",
                    "default": true,
                    "description": "Ob der Server eingehende Kontaktformulare annimt"
                },
                "serverSave": {
                    "type": "boolean",
                    "default": true,
                    "description": "Ob der Server eingehende Kontaktformulare auch abspeichert"
                }
            },
            "required": ["serverAccept", "serverSave"]
        },
        "checkAuth": {
            "type": "boolean",
            "default": false,
            "description": "Checks Authentication"
        },
        "cookieBanner": {
            "type": "boolean",
            "default": true,
            "description": "Activates the cookie banner"
        }
    },
    "required": ["accept_kontakt_msgs", "checkAuth", "cookieBanner"]
}